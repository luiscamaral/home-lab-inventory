name: reverse-proxy-2

# Second nginx reverse proxy on dockerserver-1 for HA.
# Shares the same vhost.d + cert config from NFS as rproxy-1 on dockermaster.
# All upstreams are macvlan IPs (no rproxy bridge dependency).

networks:
  servers-net:
    name: docker-servers-net
    external: true
  rproxy:
    name: rproxy
    external: true

services:
  nginx-rproxy:
    image: nginx:1.29-otel
    container_name: rproxy-2
    hostname: rproxy-2
    networks:
      servers-net:
        ipv4_address: 192.168.59.48
      rproxy:
    expose:
      - 80
      - 443
    command: [nginx, -c, /etc/nginx/nginx.conf, -g, 'daemon off;']
    volumes:
      - /nfs/dockermaster/docker/nginx-rproxy/config/conf/nginx.conf:/etc/nginx/nginx.conf:ro
      - /nfs/dockermaster/docker/nginx-rproxy/config/cert:/etc/nginx/cert:ro
      - /nfs/dockermaster/docker/nginx-rproxy/config/vhost.d:/etc/nginx/vhost.d:ro
      - /nfs/dockermaster/docker/nginx-rproxy/www:/var/www
      - /nfs/dockermaster/docker/nginx-rproxy/logs/nginx-2:/var/log/nginx
    restart: on-failure:5
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost/"]
      interval: 5s
      timeout: 5s
      retries: 15
    labels:
      loki.logging: "true"
      com.centurylinklabs.watchtower.enable: "false"
      com.docker.stack: "reverse-proxy-2"
      com.docker.service: "nginx-rproxy"
      portainer.autodeploy: "false"
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '0.5'
          memory: 1G
