name: twingate-B

networks:
  servers-net:
    name: docker-servers-net
    external: true

services:
  twingate:
    container_name: twingate-golden-mussel
    # Image updates handled by watchtower (label below); skip pull on every
    # redeploy to avoid transient registry "Proxy failure" errors during
    # `compose up`. Image cache survives container removal.
    pull_policy: missing
    image: twingate/connector:1
    hostname: golden-mussel
    networks:
      servers-net:
        ipv4_address: 192.168.59.24
    # See twingate-a.yml for rationale — macvlan containers need explicit DNS.
    dns:
      - 192.168.48.1
      - 1.1.1.1
    environment:
      TWINGATE_NETWORK: "lamaral"
      TWINGATE_LABEL_HOSTNAME: "golden-mussel"
      TWINGATE_LABEL_DEPLOYED_BY: "dockerserver-2"
      TWINGATE_ACCESS_TOKEN: ${TWINGATE_ACCESS_TOKEN}
      TWINGATE_REFRESH_TOKEN: ${TWINGATE_REFRESH_TOKEN}
    restart: unless-stopped
    labels:
      com.centurylinklabs.watchtower.enable: "true"
      com.docker.stack: "twingate-golden-mussel"
      com.docker.service: "twingate-connector-1"
      portainer.autodeploy: "false"  # Prevent auto-deploy to avoid runner disruption
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
