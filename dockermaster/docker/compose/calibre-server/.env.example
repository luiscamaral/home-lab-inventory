# Calibre Environment Configuration
# Copy this file to .env and configure the values for your environment

# Security Configuration
CALIBRE_PASSWORD=your_secure_password_here

# User and Group Configuration
# PUID - User ID for file permissions (default: 1027 for calibre, 1000 for calibre-web)
PUID=1027
# PGID - Group ID for file permissions (default: 65539 for calibre, 1000 for calibre-web)
PGID=65539

# Timezone Configuration
# TZ - Timezone for the containers (default: America/Denver for calibre, Etc/UTC for calibre-web)
TZ=America/Denver

# Notes:
# - CALIBRE_PASSWORD is required and has no default value
# - PUID/PGID should match your NFS mount permissions (1027/65539 for NAS user)
# - TZ should match your local timezone for proper scheduling and logs
# - Calibre-web service will use PUID/PGID but may override with different defaults
