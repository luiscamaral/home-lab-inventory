# LiteLLM Environment Configuration
#
# IMPORTANT: To use this configuration:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in the actual values below
# 3. Uncomment the env_file section in docker-compose.yml
# 4. Keep .env in .gitignore to prevent committing secrets
#
# Note: Environment variables can also be set directly in the shell
# or passed via docker-compose command line

# PostgreSQL Database Password (REQUIRED)
POSTGRES_PASSWORD=your_secure_postgres_password_here

# LiteLLM API Keys (add as needed)
# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# AZURE_API_KEY=your_azure_api_key_here

# Optional: Master Key for LiteLLM Proxy
# LITELLM_MASTER_KEY=your_master_key_here

# Optional: UI Admin Password
# UI_PASSWORD=your_admin_password_here
