name: Simple Runner Test

on:
  push:
    branches:
      - github-runner-cicd
    paths:
      - '.github/workflows/runner-test-simple.yml'

jobs:
  test:
    name: Test Self-hosted Runner
    runs-on: [self-hosted, dockermaster]
    timeout-minutes: 2
    
    steps:
      - name: Runner Test
        run: |
          echo "🎉 SUCCESS: Self-hosted runner is working!"
          echo "Runner: ${{ runner.name }}"
          echo "Hostname: $(hostname)"
          echo "Docker version:"
          docker version --format "Server: {{.Server.Version}}"
          echo "Current directory: $(pwd)"
          echo "Date: $(date)"